Instructions

1. Please replace the executable_path of chromedriver.exe as follows:
    Install chrome driver for your supporting chrome version from following url
    https://chromedriver.chromium.org/downloads and locate it into a folder
2. Specify the folder path in all the files to the statement specified as below
   For example,
   driver = webdriver.Chrome(executable_path="D:\sw\chromedriver_win31 (1)\chromedriver.exe")
   Here, it creates a webdriver object for performing automation testing in chrome browser.
